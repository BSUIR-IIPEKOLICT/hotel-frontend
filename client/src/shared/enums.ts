export const enum paths {
  admin = '/admin',
  login = '/login',
  register = '/register',
  user = '/user',
  main = '/',
  room = '/room',
}

export const enum roles {
  admin = 'admin',
  client = 'client',
}
