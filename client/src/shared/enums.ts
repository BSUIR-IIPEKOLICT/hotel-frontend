export const enum paths {
  create = '/create',
  login = '/login',
  register = '/register',
  basket = '/basket',
  main = '/',
  room = '/room',
}

export const enum roles {
  admin = 'admin',
  client = 'client',
}
