export const enum paths {
  admin = '/admin',
  login = '/login',
  register = '/register',
  user = '/user',
  main = '/',
  room = '/room',
  manageBuildings = '/buildings',
  manageRooms = '/rooms',
  manageServices = '/services',
  manageTypes = '/types',
}

export const enum roles {
  admin = 'admin',
  client = 'client',
}
